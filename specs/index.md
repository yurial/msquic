# Specification Index

| Path | Reference | Status | Summary |
|---|---|---|---|
| specs/bandwidth.md | bandwidth | stable | Bandwidth shaper: credit allowance, strict/continuous modes, per-call Mtu, parent hierarchy, send-path pacing |
| specs/ingress-rate.md | ingress-rate | draft | Ingress rate limiting via QUIC extension: transport parameter + RATE_LIMIT frame, applied by the peer's existing outgress throttler (bandwidth shaper) |
| specs/ingress-window.md | ingress-window | draft | Ingress shaping by delivery-driven flow-control credit grants: per-stream sliding-window rate estimation (10 ms measurement intervals, 100 ms window of the last 10 closed intervals, rate = window bytes / window time, deterministic decay to exact zero within 100 ms; no RTT), limit-anchored jitter knee, two-condition coalesced MAX_DATA/MAX_STREAM_DATA emission (10 ms cadence or 1/4-limit fill), per-connection and per-stream outstanding-window ceilings, no protocol change |
| specs/ingress-window-e2e-test.md | ingress-window-e2e-test | draft | End-to-end loopback test spec for the ingress-window shaper: server paced by the bandwidth shaper sends framed control-command streams to the limit-carrying client; derived per-interval received/delivered bounds (L + 1×D in k=0 phases, L + 2×D generally), mode matrix (conn-only/stream-only/min-semantics both), burst no-overshoot, statistical throughput bands with formula-derived tolerances; owner extension R14/R15 adds full pause/resume coverage (connection and stream level via pause phases — PHASE_BEGIN kind 4 packing duration+target, exact while-paused byte bounds, announcement-closure blocked-counter semantics (structurally 0, report-only), lump-sum resume transient inside the band's structural members, multi-stream sibling isolation, strict k̂ decay at the pause phase's own post-resume closure — deterministic rate = 0 after ≥ 10 empty closures under the window estimator) and full outgress-throttler coverage (mandatory band as pacer verification, burst paced under a server output cap with a strict two-sided band and SentRate-anchored total-time bounds, client-side egress cap session, mid-phase runtime cap change restoring pace); standalone two-process iwpair-server/iwpair-client tools (S-requirements) run the same scenario cross-machine over a real network: minimal CLI per side (address + local `-network-output-bandwidth` egress cap, not transmitted), server-commanded session configuration over the control protocol (SET_LIMITS ingress limits/strict/deadline with applied-value CONFIG_ACK), server cap emulates channel width for the data direction (pace clamped to cap, burst paced at cap, optional token-bucket burst budget via -network_output_bandwidth_burst on both sides, rate-gated, unset = prior behavior), report-stream byte-equality extension, RTT-independent mandatory asserts (incl. while-paused bounds), server-flagged strict mode for bands/blocked signatures, always-on CSV output; with no -script the server runs a built-in suite of five CI-mirror profiles (four limit configurations + connection pause; per-session SET_LIMITS, client auto-reconnects on IWP_CLOSE_NEXT, exits 0 on IWP_CLOSE_SUITE_DONE); owner extension R16 adds the built-in client EXPECTATION REPORT for arbitrary scripts: an expectation registry (per-step check rows built from the observed script + applied limits) that is the single source consumed by the S8/S9 asserts (no second copy of formulas), rendered at session end as a human per-step table (step → check (meaning) → ideal → allowed interval → actual → %deviation → verdict; verdicts PASS/FAIL/OBSERVATION/N-A) plus `iwpair,check,...` CSV lines appended after the legacy output; ideals follow the owner convention (pace = effective r_p, cap-limited = cap via a minimal burst plan-rate record extension in PHASE_BEGIN param_b that also fixes the client/server burst-deadline symmetry under caps, burst time = volume/rate, byte equality = expected payload at 0.00%, idle = 0 bytes), combinations without a derived interval (e.g. burst right after pace/pause without idle reset, short-idle decay misses, second burst, stream-scope in multi-stream, transport-honesty L=0) are enumerated explicitly and print actual + "interval not defined" with no assert, permanent report-only checks (pause onset, sibling continuation, sub-floor band) render as observations, and the registry is pinned by golden-number unit tests on injected time; standalone server extension S13: -script-file session lists (one -script-grammar line per session, # comments full-line/trailing, optional per-line labels used in the plan print and summary, replaces the built-in suite, mutually exclusive with -script, file:line load errors before any connection) and shipped network profile files src/tools/iwpair/profiles/net-*.txt (1 Mbit..100 Gbit channel widths — pace at width + 800 ms decay idle + time-budgeted capped burst, each paired by convention with -network-output-bandwidth = width/8 and -burst-ref-rate = cap in the file's header); owner follow-up: script-file lines carry an optional per-line limits segment L:<conn_mbit>:<stream_mbit> (bytes = mbit x 12500 = bandwidth x the shaper's 100 ms estimator window, exact for integers, 0 = unset, last segment, parse errors at file:line) that OVERRIDES the -client limit flags for that session via the existing per-session SET_LIMITS (no wire change; file-only — the suite keeps fixed limits, -script rejects the token); the seven profiles carry per-width values (conn_mbit = the width — window = one estimator window of channel, k-hat = 1 edge, B2 decay derived set-wide via the deterministic window zero after 10 empty closures; stream limits on net-10mbit/net-100mbit keep B< coverage); and the iwpair client's D/R observation grid becomes a fixed 10 ms (IWP_CLIENT_BUCKET_NSEC — report granularity only: the shaper's estimator is a separate constant family, redesigned to 10 ms intervals over a 100 ms window; k-hat replay/eligibility follow the estimator's own closures and the gtest 100 ms grid is unchanged; interval bounds evaluated per 10 ms bucket in the same scale-invariant forms, bucket trace ~10x) |
